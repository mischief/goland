config = {
  -- map file
  map         = "map",
  scriptpath  = "../scripts/?.lua",

  -- listen dialstring
  listener    = "127.0.0.1:61507",

  -- logging & debugging
  logfile     = "server.log",

  debug       = "false",
  --cpuprofile  = "server.profile",
}

return config
