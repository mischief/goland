config = {
  -- map file
  map         = "map",
  scriptdir   = "../scripts",

  -- listen dialstring
  listener    = "127.0.0.1:61507",

  -- logging & debugging
  logfile     = "server.log",

  debug       = "true",
  --cpuprofile  = "server.profile",
}

return config
