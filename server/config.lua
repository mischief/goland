config = {
  -- map file
  map         = "map",

  -- listen dialstring
  listener    = "127.0.0.1:61507",

  -- logging & debugging
  logfile     = "server.log",

  debug       = "true",
  --cpuprofile  = "server.profile",
}

return config
